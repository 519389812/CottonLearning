import { _decorator, Component, Node } from 'cc';
const { ccclass, property } = _decorator;

// 挂载在node上，将node标记为CircleManager类型，添加到H2AGameManager中
@ccclass('CircleManager')
export class CircleManager extends Component {
}


